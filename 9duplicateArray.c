#include<stdio.h>
#include<conio.h>

int duplicate(int arr[],int n)
{
   for(int i=0;i,n;i++)
   {
       if(arr[i]==arr[i+1])
   }
}

int main()
{
    int arr[]={6,9,2,1,2};
    duplicate();
    return 0;
}